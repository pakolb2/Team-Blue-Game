"""
server/game/__init__.py
-----------------------
Game engine package.

Submodules:
    deck      — build, shuffle, deal
    rules     — legal move validation        (Phase 4)
    scoring   — trick & game scoring         (Phase 4)
    engine    — main game loop               (Phase 5)
    variants/ — per-variant rule classes     (Phase 3+)
"""
